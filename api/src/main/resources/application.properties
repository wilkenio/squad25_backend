# #producao

# spring.application.name=api

# spring.datasource.url=jdbc:postgresql://localhost:5432/financeiro
# spring.datasource.username=postgres
# spring.datasource.password=root

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# api.security.token.secret=secret-key

# # Configurações para HTTPS com o keystore
# server.port=8443
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=squad25ovencedor
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=minhaapi


#localhost
spring.application.name=api

spring.datasource.url=jdbc:postgresql://localhost:5432/financeiro
spring.datasource.username=postgres
spring.datasource.password=banco123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

api.security.token.secret=secret-key

server.port=8080
server.ssl.enabled=false
